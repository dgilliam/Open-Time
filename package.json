{
  "name": "open-time",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "dev": "next dev",
    "build": "next build",
    "start": "next start",
    "test": "vitest run",
    "seed": "tsx scripts/seed.ts",
    "backup": "tsx scripts/backup.ts",
    "sync": "bash scripts/dev-sync.sh"
  },
  "dependencies": {
    "better-sqlite3": "^11.10.0",
    "next": "^15.5.23",
    "react": "19.1.0",
    "react-dom": "19.1.0"
  },
  "//overrides": [
    "next 15.5.x pins postcss 8.4.31 and sharp 0.34.5, both still inside their",
    "advisory ranges (GHSA-6g55-p6wh-862q / -r28c-9q8g-f849 / -qx2v-qp2m-jg93 /",
    "-fxqj-rqcc-2cmp, GHSA-f88m-g3jw-g9cj). npm's only offered fix is next@16, a",
    "major. These overrides take the patched versions inside the same major line",
    "instead. nanoid comes in under postcss and needs >=3.3.18 (GHSA-28wg-ghj8-5hjv,",
    "GHSA-2v37-7h3g-55p8). Drop these once next 15.x ships the bumps itself.",
    "postcss is build-time only (globals.css, no custom config) and sharp is only",
    "reachable through /_next/image — this app never imports next/image."
  ],
  "overrides": {
    "postcss": "^8.5.26",
    "sharp": "^0.35.3",
    "nanoid": "^3.3.18"
  },
  "devDependencies": {
    "@types/better-sqlite3": "^7.6.12",
    "@types/node": "^22.10.5",
    "@types/react": "^19.0.7",
    "@types/react-dom": "^19.0.3",
    "tsx": "^4.19.2",
    "typescript": "^5.7.3",
    "vitest": "^4.1.10"
  }
}
